version: "3.7"
services:
    frontend:
        build:
            context: ./frontend
            dockerfile: Dockerfile.dev
            target: development
        volumes:
            - ./frontend:/app
            - /app/node_modules
        ports:
            - "5173:5173"
    server:
        container_name: ng-server
        build:
            context: ./server
            dockerfile: Dockerfile.dev
            target: development
            args:
                NODE_PORT: 3000
        volumes:
            - ./server:/usr/server
            - /usr/server/node_modules
        ports:
            - "3000:3000"
